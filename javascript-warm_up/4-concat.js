#!/usr/bin/node

// Store the first argument passed to the script
const firstArgument = process.argv[2];

// Store the second argument passed to the script
const secondArgument = process.argv[3];

// Print both arguments with "is" between them
console.log(firstArgument + ' is ' + secondArgument);
