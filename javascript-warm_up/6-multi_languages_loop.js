#!/usr/bin/node

// Store the three required sentences in an array
const languages = [
  'C is fun',
  'Python is cool',
  'JavaScript is amazing'
];

// Loop through the array and print each sentence
for (const language of languages) {
  console.log(language);
}
